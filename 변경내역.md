# 인도네시아 일일보고 자동화 — 변경 내역 (2026-08-04)

작업파일 6개·로데이터 2개·주간추정 1개로 도구를 실측 검증한 뒤, 발견된 결함을 고치고
관리회계 확정 실적 반영 기능을 추가했다.

## 1. 신규 기능 — 관리회계 확정 실적 반영 (분기·기누적)

- **문제**: 분기·기누적을 원천 raw로만 더해 집계 시트(관리회계 확정치)와 차이가 났다.
  특히 IB FEE·IB BOOK 누적은 대폭 어긋났다. (검증 §9.3a)
- **해결**: `확정 실적` 탭을 신설. 월별·보고항목별 원화 억원을 넣으면
  - **마감월**은 확정치(또는 추정치)로 고정,
  - **당월**은 마감 전까지 원천 일데이터를 누적,
  - **분기·기누적** = Σ(마감월 확정) + Σ(미마감월 raw) + 당월 일누적 으로 산출.
  - 관리회계 2단계(추정→확정)를 월별 `단계` 선택으로 지원. 확정이 나오면 단계만 바꿔 덮어씀.
  - 값을 비우거나 `미마감`이면 그 달은 예전처럼 raw로 계산(동작 보존).
  - **금일·주간·월간·M/S·잔고에는 영향 없음.**
- 입력 방식: 셀 직접 입력 + 붙여넣기(`YYYY-MM 단계 BK AM IB_FEE 신용 예금 SW IB_BOOK 기타`).
- 저장: `close_ledger.json` (일괄 저장에 포함, 드롭/열기로 재적재).
- `점검 결과`에 확정 실적 반영 현황 표시, `계산 결과`에 "분기·기누적: 확정 실적 반영" 배지.

## 2. 결함 수정 — 월요일/연휴 보고 "전일" 잔고 붕괴

- **문제**: 보고일이 월요일(또는 연휴 다음)이면 원천 상한 R이 주말/휴일이라
  `prevDataDay(R)`가 금일과 같은 날로 붕괴 → 신용대출 "전일" 열·계좌 일간증감·신용증감 금일이 0/오류.
  (8/3 작업파일: 전일 소계 438.389인데 도구는 432.98을 냈음)
- **해결**: 전일을 R이 아니라 *금일이 실제로 해석된 자료일*의 직전 자료일로 잡도록 수정.
  → 8/3 전일 소계 438.389, 총계좌 전일 92,540로 작업파일과 일치. 평일 동작 불변.

## 3. 결함 수정 — 저장한 `fx_ledger.json` 재적재 불가 (브라우저 기능검사에서 발견)

- **문제**: 파일 라우팅이 파일명에 `fx`가 들어가면 무조건 환율 **엑셀** 리더로 보내,
  저장해 둔 `fx_ledger.json`(JSON 원장)이 `XLSX.read`로 잘못 넘어가 "IDRKRW 시트를 찾지 못했습니다"로 실패했다.
  → 어제 저장한 환율 원장을 다음 날 다시 올릴 수 없었다.
- **해결**: 환율 엑셀 분기를 `.xlsb`이거나 *스프레드시트 확장자일 때만* 적용하도록 제한.
  JSON은 `고시` 필드로 정상 인식. → 재적재 확인("환율 원장 적재: 2473일").

## 4. 결함 수정 — 월요일 "주간" 구간

- **문제**: 월요일은 금일이 전주 금·토·일까지 담는데 주간 구간은 `[월요일-1, R]`이라
  그 3일을 제외 → 주간이 0에 가깝게 나왔다. (8/3 BK 주간 도구 0 vs 작업파일 0.1446)
- **해결**: 주간 시작을 금일 구간 시작보다 늦지 않게 정렬 → 8/3 주간 8/8 작업파일 일치. 평일 불변.

## 검증

### 계산 로직 (Node + 원천 실행)
- 연속 적재/변경 감지: 7/30→7/31 rev 17건 변경, 재투입 0건.
- 순영업수익 합계(계수 전) 761.94 = 원천 nor, §8.2 원천값 7종 정확.
- 금일 8항목: 7/28·7/29·7/30 = 8/8, 7/31 = 7/8(추정, IB BOOK 0.0052), 8/3 = 8/8.
- 잔고: 평일(7/29) 467.688/458.436, 월요일(8/3) 금일 432.98/전일 438.389 모두 작업파일 일치.
- 확정 blend: 마감월 확정 + 당월 일누적 정상, 확정 제거 시 raw로 원복(무회귀).

### 브라우저 기능검사 (Playwright / Chromium, 실제 DOM)
- 파일 드롭 라우팅, 탭 전환, 배지, 진행단계 6칸, 휴일 체크박스 정상.
- 확정 실적 탭: 월별 그리드·붙여넣기·단계 선택·6개월 배지·계산결과 반영 배지 정상.
- 저장: `ledger·fx_ledger·holidays·close_ledger·form_config`(+추정) 실제 파일 생성.
- PNG(415KB, foreignObject→canvas)·검산 엑셀(5시트)·보고양식 엑셀(직접 만든 zip) 모두 유효하게 열림.
- 창닫기 경고 활성.
- (참고) 주간 추정 파일 라우팅은 파일명의 한글(순영업수익/아시아사업)에 의존 → Playwright가
  한글 파일명 첨부를 못 해 브라우저 검사는 건너뛰고 Node로 검증(정규식 매칭 + importEst 46일).

## 추가 개선 (2026-08-04, 2차)

- **과거 환율 HTML 내장**: 2017~2026 IDRKRW 2,473일을 도구에 기본 내장. 파일 없이 열어도 과거 환율이
  바로 적재되고 환율 탭이 표시됨. 파일 업로드나 당일 환율 입력으로 언제든 덮어쓸 수 있음.
- **환율 넣기 탭**: 당일 환율 입력 유지, 파일 업로드 시 파일 값으로 전체 교체.
- **연도별·월별 환율 표 개편**: 과거 연도는 각 연도 기누적환율(연평균)만, 당해(2026)는 월별 + "2026 기누적"
  요약행으로 표시. (7/24 11.624166880·7/31 11.650361968 무회귀 확인)
- **저장 위치 안내**: 보고양식 PNG·엑셀 저장 시 파일명과 "다운로드(Downloads) 폴더" 위치를 토스트로 안내.
- 로드 즉시 `recalc()`로 내장 환율을 화면에 반영. 계산·확정 실적·월요일 결함 수정 전부 무회귀.

## 추가 개선 (2026-08-04, 3차)

- **보고양식 자리수 통일** (화면·엑셀 동일):
  - 순영업수익 실적 등 억단위 금액 → 소수 **2자리**
  - 달성률·M/S 등 % → 소수 **1자리**
  - 신용대출 말잔(신용·대출·소계·본사대출) → **정수**(억, 소수 없음)
  - 가중평균금리 → 소수 **2자리** %(0.00%)
  - 엑셀 서식(numFmt 168=0.00% 신설)까지 반영, openpyxl로 서식 검증.
- **사용설명서 갱신**: 과거 환율 내장(따로 안 넣어도 됨)·저장 위치(다운로드 폴더) 안내 반영. XSD 검증 통과.

## 추가 개선 (2026-08-04, 4차) — 관리회계 확정실적 내장

- **확정실적 HTML 내장**: Back 집계시트(일일실적 집계)의 확정 블록(1~6월)을 도구에 기본 내장.
  1~3월 확정은 집계 시트 기준 1월 행에 합산 기재(2·3월 0), 4·5·6월은 개별. 파일 업로드로 덮어쓸 수 있음.
  → 분기·기누적이 회계마감 확정치를 자동 반영(BK 기 35.11 등, 집계 시트 기준).
- **회계마감 이전(raw) 기누적 표기**:
  - 계산 결과 탭 맨 아래에 "기누적 (회계마감 전·raw)" 행 추가(확정 미반영 원천 누적).
  - "확정 반영 vs 회계마감 전(raw)" 항목별 비교 카드 추가.
  - 확정 실적 탭 맨 아래에 "확정 누적(마감월 합)" 행 추가.
- 내장 확정 로드 시 확정 배지·당월 판정이 최신 환율월 기준으로 동작하도록 보정.
- 금일 8/8·월요일 결함·환율 기누적·자리수 전부 무회귀.

## 추가 개선 (2026-08-04, 5차) — 확정실적: 기본 내장 + 사용자 수정 가능

- 확정 실적 탭에 **"기본값 내장 + 직접 수정 + 파일 저장/재적재로 유지"** 안내를 명시.
- 편집 흐름: 내장 기본값 → 그리드에서 직접 수정 → **[확정 실적 파일 받기]**로 `close_ledger.json` 저장 →
  다음에 그 파일을 올리면 수정값 유지(내장값 완전 대체). 업로드 파일이 항상 우선.
- 편집용 동봉 파일 제공: `close_ledger.json`(확정실적 기본값), `fx_ledger.json`(환율 기본값).
- E2E: 셀 편집→저장→재적재 전 과정 확인.

## 추가 개선 (2026-08-04, 6차) — 병합 업로드 + 인니 휴장일 기본 등록

- **업로드 병합 방식**: 확정실적(close_ledger)·환율(fx_ledger)을 올릴 때 <b>올린 항목(달/일자)만 덮어쓰고
  나머지는 내장/기존값을 유지</b>(전체 대체 아님). 확정실적 삭제는 표에서 단계를 '미마감'으로.
- **인도네시아 휴장일 기본 등록**: 원천 인도네시아휴일 시트 기준 2026년 22일을 도구에 기본 내장.
  휴일 탭(인도네시아 선택)에서 추가·수정·삭제·저장(holidays.json) 가능. 주간 경과영업일 계산에 반영.
- 확정 실적 탭 안내를 '병합' 기준으로 수정. 금일 8/8·월요일·환율·자리수 무회귀.

## 추가 개선 (2026-08-04, 7차) — 임시환율 기본값 + 미입력 필수칸 강조

- **임시 환율**: 기본값(11.6242)을 없애고 비워 둠. 비어 있으면 <b>전일자(가장 최근) 환율</b>을 자동 사용,
  값을 넣으면 그 값(당일)을 사용. placeholder에 "미입력 = 전일자 환율" 안내.
- **미입력 필수칸 강조**: 보고 양식의 필수 입력칸이 비어 있으면 <b>주황 테두리·배경</b>으로 강조하고
  상단에 "입력 필요 N칸" 배너 표시. 값을 넣으면 즉시 해제. (기본값 있는 칸은 제외 → 신용 계좌수 등 실제 미입력만 표시)
- 무회귀: 금일 8/8·월요일·환율 기누적·자리수 전부 유지.

## 추가 개선 (2026-08-04, 8차) — 환율 탭 정리

- **일자별 적재 내역**: 최근 90일 제한을 없애고 <b>당해년도(2026) 전체</b>를 표시(144일).
- **연초 휴장일(1/1) 처리**: 환율이 없는 연초 휴일은 가장 이른 기누적환율로 앞채우고
  "환율이 없는 일자" 경고에서 제외. (2026-01-01은 휴장일이라 데이터가 없어도 정상)
- **"데이터 없다" 표시 수정**: 휴일·주말이 아닌 진짜 결측만 경고하도록 변경. 상단 배너 경고도 함께 정리.
- 무회귀: 금일 8/8·월요일·환율 기누적(주차고정)·자리수 전부 유지.

## 추가 개선 (2026-08-04, 9차) — 기본 보고일 = 당일 단일 보고

- **문제/요청**: 일일보고는 당일 실적을 보고하는 것이므로, 원장에 그 이후 데이터가 있어도
  디폴트는 당일까지만 써야 한다. 기존 기본 보고일은 최신 자료일의 *다음 영업일*로 주말을 건너뛰어
  월요일로 앞서갔고, 그 결과 금요일이 최신인데도 금일이 "금~일" 3일치(주간단위)로 표시됐다.
- **해결**: 기본 보고일을 최신 자료일의 **다음 날(+1일)** 로 바꿔 원천 상한 R = 최신 자료일이 되게 했다.
  → 금일은 그 하루만 담는 **단일일 보고**가 되고, 그 이후 데이터는 쓰지 않는다.
  사용자가 보고일을 뒤(예: 월요일)로 직접 지정해 다시 계산하면, 그때만 전주 금·토·일을 담는
  **주간단위**로 확장된다(월요일 연휴 이월 로직은 그대로 유지).
- **무회귀**: 고정 보고일(7/28~8/3)의 금일·주간 구간 불변, 월요일 8/3 작업파일 일치값
  (금일 8/8·주간 0.1446·전일 438.389·총계좌 92,540) 그대로. 확정 blend·환율 기누적·자리수 전부 유지.
  (검증: 최신 7/31(금) → 기본 asOf 8/1(토)·R=7/31·금일 단일 / 사용자 8/3(월) 지정 시 금일 7/31~8/2 확장)
- 사용설명서 '보고할 날짜' 설명에 기본 단일일 보고·주간 확장 규칙을 추가.

## 추가 개선 (2026-08-04, 10차) — 기본 보고일을 '당일(오늘)' 기준으로 고정

- **문제**: 원장에 당일(오늘) 이후의 미래 날짜 데이터까지 들어 있으면, 기본 보고일이 그 미래 최신
  자료일을 기준으로 잡혀 미래 실적을 보여줬다(원장 최신일자에 무조건 붙는 로직).
- **해결**: 기본 보고일 기준을 <b>당일(오늘) 이하의 최신 자료일</b>로 바꿨다.
  `effLatestDay(all, todayStr())` = 오늘 날짜(`new Date()`) 이하의 가장 최신 자료일을 골라 그 다음 날을 기본 보고일로 한다.
  → 원천에 미래 날짜가 있어도 무시하고 항상 당일 기준으로 단일일 보고한다.
  범위 밖 보정(recalc의 out-of-range guard)도 같은 기준(effLast)으로 통일.
- 사용자가 보고일을 직접 미래로 지정하는 것은 그대로 허용(수동 입력 우선).
- **무회귀**: 미래 데이터가 없으면 종전과 동일(최신 자료일+1). 고정 보고일·월요일 이월·확정 blend·
  환율 기누적·자리수 전부 유지. (검증: 오늘 8/4·원장 7/31+미래 8/10·8/17 → 기본 7/31 기준, 미래 무시)
- 사용설명서 '보고할 날짜' 설명에 '당일 기준·미래 날짜 무시'를 명시.

## 추가 개선 (2026-08-04, 11차) — 저장 파일명 정리(내용 기반 이름 + 보고일 6자리)

- **요청**: 저장 파일명에 날짜 6자리(YYMMDD)를 붙이고, 무엇을 저장하는지 더 짧고 명확하게.
- **변경**: 6종 상태 파일 이름을 내용 중심 한글로 바꾸고 보고일 6자리를 접미로 붙였다.
  - `ledger.json` → `원장_YYMMDD.json`
  - `fx_ledger.json` → `환율_YYMMDD.json`
  - `holidays.json` → `휴일_YYMMDD.json`
  - `est_ledger.json` → `주간추정_YYMMDD.json`
  - `close_ledger.json` → `확정실적_YYMMDD.json`
  - `form_config.json` → `설정_YYMMDD.json`
  - 리포트 내보내기도 통일: `KISI_일일보고_YYMMDD.xlsx` · `일일보고_YYMMDD.png` · `적재검증_YYMMDD.xlsx`
  - 날짜는 보고일(asOf) 기준 `stamp6()`, 계산 전이면 오늘 날짜로 대체.
- **안전**: 개별 저장 버튼·일괄 저장(오늘 작업 저장) 모든 경로에 반영. 재적재는 파일명이 아니라
  **내용**(고시·단계·한국/인니·i_msT 등)으로 인식하므로 이름이 달라도 그대로 읽힌다(무회귀 확인).
- **인니 휴장일**: 기본 등록(내장 22일) + 휴일 탭에서 추가·수정·삭제 유지(변경 없음).
- 사용설명서·README의 저장 파일 이름 안내를 새 규칙으로 갱신.

## 추가 개선 (2026-08-04, 12차) — 인니 휴일 명칭 정규화

- **문제**: 원천 `인도네시아휴일` 시트가 `[날짜, 월약어]` 구조라 실제 휴일명이 없고, 파서가
  두 번째 칸의 월 라벨(Jan·Feb·Mar…Dec)을 명칭으로 잡아 휴일 명칭이 오염됐다(예: 2026-07-24 "Aug").
- **해결**:
  - 파서(`loadIdHolidays`)가 **월 라벨을 명칭으로 쓰지 않도록** 제외(`isMonthLabel`). 실제 휴일명이
    있으면 그것만 잡고, 없으면 `인니 휴장일`로 넣는다.
  - `normalizeIdHolNames()`로 **어떤 경로(내장·원천 시트·파일 재적재·설정 파일)로 적재되든** 인니
    명칭을 항상 정리. `renderHoliday()`·`loadIdHolidays()`에서 호출.
  - **날짜는 보존**하고 명칭만 정리(휴장일 자체는 유지). 실제 휴일명(예: 성탄절)은 건드리지 않음.
  - 기존에 저장해 둔 오염된 `holidays_*.json`을 다시 올려도 자동으로 명칭이 정리된다.
- **유지**: 인니 휴장일 기본 등록 + 휴일 탭 추가·수정·삭제 그대로.
- **무회귀**: 경과영업일 계산은 날짜·`제외`만 쓰므로 영향 없음. 금일 8/8·월요일·환율·자리수 유지.
  (검증: 내장 22건·원천 시트·사용자 오염 파일 36건 모두 정규화 후 비정상 명칭 0)

## 추가 개선 (2026-08-05, 13차) — Koyeb식 에메랄드 포인트 리스킨

- **요청**: Koyeb 페이지 느낌으로, 화면의 '녹색 포인트'만 산뜻하게. 레이아웃은 유지.
- **결정**: 버튼은 검정(Koyeb식), 그린은 포인트에만. 그린 톤 = **에메랄드 `#10B981`**.
- **변경**(모두 CSS, `:root` 토큰 기반이라 변경 최소):
  - `--accent` 남색(#1f4e79) → **에메랄드 #10b981** (활성 탭 밑줄·드롭·daybox·진행바 등 포인트).
  - `--accent-soft` → #d1fae5(연녹), 신규 `--accent-ink` #047857(밝은 배경 위 그린 텍스트, 대비 확보).
  - 주요 버튼(.btn) → **검정 #16202b + 흰 글씨**(신규 `--btn`), ghost는 흰 배경·검정 테두리(호버 시 그린).
  - 헤더 하단에 얇은 에메랄드 라인 추가.
  - **카운트 배지는 빨강 유지**(주의 신호), 성공색(--good)은 딥그린 유지.
- **무회귀**: CSS만 변경 → 계산·저장·엑셀·휴일 로직 영향 없음. 남색 하드코딩 0건 확인,
  Chromium 렌더로 에메랄드 포인트·검정 버튼·빨강 배지 확인.

## 추가 개선 (2026-08-05, 14차) — 정답지(세부일일보고) 대조: 주말 규칙 통합

- **대조**: 8/5 로데이터로 도구를 돌려 정답지 세부일일보고와 실측 대조. **금일 0.4981 정확 일치**.
- **사용자 확정 규칙 반영**:
  - **주간 = 토·일·월·화·수·목·금**(토요일 시작 주). `buildPeriods` wkFrom = 해당 주 토요일.
  - **토·일 = 직전 금요일 값, 단 BK·AM·IB FEE·SW = 0** (신용·예금·IB BOOK·기타·미수·주담은 금요일 값 유지).
  - 월간·분기 구간 경계의 `-1`(전월말 포함) 제거 → 월 1일/분기 1일 시작.
- **구현**: 신규 `dayFlowRaw()`로 주말/미도착일 규칙을 **단일 접근자**에 통합. 기존 `sumEokCarry`/`CARRY_ROWS`
  이월 로직(규칙과 모순: SW 이월·미수/주담 누락)을 대체 → 이중 이월 제거. 원장 원본·저장 파일 무변경.
- **결과(정답 대비)**: 금일 0.4981(정확), 주간 1.26→**1.86**(정답 2.00), 분기 17.52(정답 17.05), 기 116.28(정답 115.81).
  주간이 크게 개선됨. 잔차(주간 -0.15, 분기·기 +0.47)는 도구의 일자별 원천 추출값이 마스터 재가공값과
  미세하게 다르고(특히 7월 raw vs 관리회계 확정), 완전 일치엔 마스터의 세부 일자값이 필요 — 문서화.
- **무회귀**: 평일 금일(7/28~7/30) 8/8, 전일 잔고 438.389·총계좌 92,540 유지. 8/3(월)은 **추정치 파일**이라
  확정 규칙 적용 시 SW 주말=0으로 1개 포켓만 달라짐(추정→확정 차이, 정상).
- **기본 보고일 = '오늘'(R=전일)로 교정**: 로데이터에 오늘 추정치·내일 이후 예상이 들어와도, 기본 보고는
  `asOf = min(오늘, 최신자료일+1)`로 잡아 **전일(막 확정된 날)** 기준으로 만든다. → 오늘 추정·미래 예상은
  R로 자동 제외(예: 오늘 8/5·데이터 8/7까지 → 기본 asOf 8/5·R 8/4). 다음날 8/6 열면 8/5가 확정으로 들어와
  주간합계도 자동 갱신. 수동 기준일 경로는 기존대로(R=기준일-1) 정상.
- (예정) 출력 일일보고/세부일일보고 분리(화면+엑셀)는 후속.

## 추가 개선 (2026-08-05, 15차) — 구간 상한 = 보고일 '당일'(R=D) 규칙 확정

- **사용자 확정**: 보고일 T 기준 모든 구간(금일·주간·월간·분기·기)의 상한 = **T 당일**.
  - 예: 8/5 보고 → **금일=8/5(당일 추정 포함)**, **주간=월간=8/1~8/5**(토~수), **8/6·8/7 제외**.
  - 즉 오늘 만든 보고는 **당일 추정치까지 포함**하고, 내일 이후 예상만 뺀다. 다음날이면 그 날이 확정으로 들어와 누적 갱신.
- **변경**: 도구의 원천상한 `R`을 `보고일-1` → **`보고일`**로 교정(`buildPeriods` R=D, dayFrom=직전보고일+1).
  runChecks·modeInfo·범위가드·환율인자의 `asOf-1` → `asOf`. 기본 보고일 = `effLatestDay(오늘)`(오늘 이하 최신 자료일).
  (직전 턴의 'R=전일'(Part0)은 오해였어서 되돌림.)
- **결과(8/5·현 로데이터)**: 금일 0.4641(8/5), **주간=월간=2.3193(8/1~8/5)**, 8/6·8/7 제외 확인.
  잔고·M/S·환율도 당일(R=8/5) 기준으로 정렬.
- **주의**: 정답지(마스터)의 금일 0.4981은 마스터의 8/5 '추정' 값으로, 도구가 Raw data에서 뽑는 8/5(0.4641)와
  다름(원천 재가공 차이). 규칙(당일 포함·주간=월간·미래 제외)은 정확히 반영. 정밀 값은 7월 관리회계 확정 입력 시 수렴.

## 추가 개선 (2026-08-05, 16차) — 보고양식 엑셀을 '일일보고'+'세부 일일보고' 두 시트로 분리

- **요청**: 결과물을 정답지처럼 일일보고 / 세부일일보고로 분리(화면+엑셀).
- **엑셀**: `보고양식 → 엑셀로 받기`(`KISI_일일보고_YYMMDD.xlsx`)가 이제 **두 시트**로 나온다.
  - `일일보고`: 기존 보고양식(순영업수익 현황·외형/KPI·신용대출·계좌수).
  - `세부 일일보고`: 순영업수익 **포켓별 상세**(구분/구성/기목표/금일·주간·월간·분기·기) +
    **기누적(확정 반영 vs 회계마감 전 raw) 비교** + 환율(기준일 적용환율).
  - 구현: `makeXlsx`를 다중시트 `makeXlsxMulti`로 일반화(Content_Types·rels·workbook 시트 태그 확장), `detailSheetXml` 신설.
  - 검증: SheetJS·openpyxl 양쪽에서 두 시트 정상 로드(`일일보고` A1:L41, `세부 일일보고` A1:H31).
- **화면**: `보고 양식` 탭 = 일일보고, `계산 결과` 탭 = 세부(포켓별 상세 표)로 이미 분리 표시됨.
- 파일명 규칙(stamp6)·서식(자리수)·기존 계산 무영향.

## 참고 사항
- 월별 확정 억원은 관리회계 파일(예: `주차별 손익(확정)` 시트)에서 포켓별로 옮겨 넣는다.
- 사용설명서(docx)는 저장 파일 6개·확정 실적 탭 반영해 갱신함.

## 추가 개선 (2026-08-05, 17차) — 주말 처리: 원천 주말행 우선(과다합산 제거)

- **문제**: 도구가 토·일을 무조건 '직전 금요일 값'으로 덮어써(특히 IB BOOK 평가손익) 주간·분기·기를 과다 계상.
- **해결**: 원천에 주말행이 있으면 **원천 값을 그대로 사용**(원천이 이미 규칙대로 BK·AM·IB FEE·SW=0으로 채워둠),
  원천에 주말행이 없을 때만(월요일 등 미도착) 직전 금요일 값으로 보충. `dayFlowRaw` 조정.
- **정답 대조(8/5)**: 금일 0.4641(정답 0.4981)·주간 1.9547(2.0017)·분기 16.99(17.05)·**기 115.75(정답 115.81, 차 0.06)**.
  116.74→115.75로 정답에 거의 일치. 남은 ~0.05억은 8/5 당일 추정치 한 값 차이(도구 Raw 0.4641 vs 마스터 추정)로 전 구간 전파.

## 추가 개선 (2026-08-05, 18차) — 정답지 방식(혼합 기준) 채택 + 정답 대조 확정

- **사용자 선택(A)**: 금일 = **전일(D-1, 확정)**, 주간·월간·분기·기 = **당일(D, 추정 포함)**. (정답지 방식)
- **구현**: `buildPeriods`에 상한 2종 — `원천상한=D-1`(금일·잔고), `누적상한=D`(주간~기). buildReport 확정 blend도
  누적상한(당일) 사용. runChecks·라벨은 금일상한(D-1)/누적상한(D) 병기.
- **정답 대조(8/5)**: **금일 0.4981 = 정답 정확 일치.** 주간 1.9547(정답 2.0017)·분기 16.99(17.05)·기 115.75(115.81).
  누적의 잔여 ~0.05억은 **오직 8/5 당일 추정치 한 값 차이**(도구 Raw 0.4641 vs 마스터 추정 ~0.51)가 전파된 것.
  → 8/4까지 확정값은 포켓 단위로 정답지와 동일(AM 0.0316·BK 0.1677·소계 0.4981), 8/5 확정 시 완전 수렴.
- **잔고 무회귀 복원**: 전일 소계 438.389·총계좌 92,540·BK 주간 0.1446 정확(금일·잔고가 다시 확정 기준 D-1).

## 추가 개선 (2026-08-06, 19차) — 중간파일(Back_260805_vF) 대조: 당일 전일확정 이월 + 주말 환율 주차 통일

- **입력**: 사용자가 정답지 값을 만든 **중간파일**(`Back_260805_vF.xlsx`, 시트 `일일실적 집계`) 제공.
  IDR 금액은 원천 로데이터와 동일, 이 파일에서 **환율 원화환산** 후 정답지에 값복사. "환율 등 확인하여 왜 다른지 파악하라".
- **환율 확인 결과**: 도구·마스터의 **기누적환율 수치는 일치**(7월 11.6242 / 8월 11.6504). 환율 '값'은 문제 없음.
- **차이 원인(주간 0.0470억) 정량 분해**:
  1. **당일(8/5) 추정 방식(+0.0340)** — 마스터는 당일=**전일(8/4) 확정치를 그대로 이월**(8/5 전 포켓=8/4). 도구는 로데이터 8/5 원본 추정(0.4641).
  2. **주말 환율 주차 경계(≈−0.0014)** — 마스터는 8/1(토)·8/2(일)에도 **8월 기누적환율(11.6504)** 적용. 도구는 ISO주(월~일) 기준이라 주말을 전주(7월, 11.6242)에 귀속.
  3. 나머지는 위 두 가지와 미수이자 주말 배분의 상호작용.
- **사용자 확정 → 반영**:
  - **당일 = 전일 확정치 이월(금일·누적 공통)**: `dayFlowRaw`에 보고일(REPORT_DAY) 이월 규칙 신설. 누적(주간~기)에서도 당일=전일확정.
  - **주말 환율 = 주간 정의(토~금)와 동일 주로 고정**: `makeFxOf` 주차고정 기준을 ISO 월요일 → **해당 주 토요일 직전 영업일**로 변경. 토·일이 뒤따르는 평일과 같은 주 환율 사용.
  - **주간 시작 정정**: `buildPeriods` 주간 시작을 항상 **해당 주 토요일**로(월요일 보고 시 전주 금요일 중복계상 제거).
- **정답 대조(8/5) — 포켓 단위 완전 일치(AM 제외)**:
  - 금일 **0.4981 = 정답 정확 일치**.
  - BK·IB FEE·여신(신용+미수+주담)·예금·SW·IB Book·기타 → **금일/주간/월간/분기/기 모두 정답지와 소수 4자리까지 일치**.
  - **유일한 잔차 = AM**: 주간 0.1455(정답 0.1595)·분기 1.436(1.4641)·기 7.4425(7.4705). 잔차 0.014(주간)/0.028(분기·기) 전부 AM.
- **AM 잔차 원인(로데이터 대조로 규명)**: 로데이터 주말(8/1·8/2) **AM=0**(원천이 3일치를 월요일에 몰아 계상). 마스터는 여기에 **주말 AM ≈0.007억/일을 별도 가산**(로데이터에 없는 값, 사용자 규칙 'AM 주말=0'과도 상반). → 도구는 **로데이터에 충실**, 마스터의 수기 가산분만 미반영. AM 주말 계상 기준을 별도로 받으면 완전 일치 가능.
- 미수이자 주말 이연(마스터: 주말=0·월요일 몰아 계상)은 **주말 환율 통일로 여신소계가 이미 정확히 일치** → 별도 재배분 불필요.
- 무회귀: 8/5 금일·전 포켓(AM 외) 정답 일치. 월요일 보고 중복계상 해소. 잔고는 주말환율 주차 통일로 전일 소계 438.389→437.403(승인된 규칙의 정상 반영).

## 추가 개선 (2026-08-06, 20차) — 주말 AM = 원천 AM Rev(관리보수) 상수 이월 (백파일 로직 일치)

- **사용자 지시**: "원천 로데이터 76행(AM Rev 상수 8,152,613)이 존재하고 중간파일도 같은 값이므로 **주말 AM은 0이 아니라 원천 AM Rev 값을 사용**하는 것. 백파일 로직·구성을 재점검해 동일하게 처리하도록 로직 수정."
- **백파일 로직 재확인**: 집계 AM 수식 = `붙여넣기!(채권손익 row102) + 붙여넣기!(AM Rev row81)`. 원천 `Raw data_NEW` row81(AM Rev) = **평일 8.153M · 주말(토·일) 0**(소스 미posting). 백파일은 이 상수를 주말에도 유지(직전 영업일 값 이월). 순영업수익 기준 흐름(붙여넣기→집계)에서 AM 외 BK·신용·주담·예금·IB·SW는 원천과 완전 일치.
- **수정 내용** (`app2.js`):
  1. 참조 항목 `am_fee`(=AM Rev, row81) 신설 → 일자별 관리보수 상수 별도 추출.
  2. `WEEKEND_ZERO`에서 `am` 제거. `dayFlowRaw` 주말 분기에 **AM 특례**: 주말 am = 직전 영업일 `am_fee` 이월(거래성 채권손익=0) → 주말 AM = 0.007억(=8.153M). 평일·당일(전일 이월)은 현행 유지.
- **정답 대조(8/5) — 금일·주간·월간 완전 일치**:
  - 금일 **0.4981** · 주간 **2.0017** · 월간 **2.0017** = 정답지 정확 일치(AM 주간 0.1455→**0.1595** 교정).
  - 전 포켓(BK·AM·IB FEE·여신·예금·SW·IB Book·기타) 주간까지 소수 4자리 일치.
- **분기·기(17.05/115.81) 잔차 = 7월 관리회계 미확정 탓**: 분기·기는 마감월 확정 + 당월 raw로 산출하는데 **7월이 아직 미확정**이라 7월 raw 추정을 사용. 마스터는 7월 **확정**(AM 회계전 1.3046)을 씀 → 도구 분기 AM 1.5348(=7월raw 1.3753+8월 0.1595) vs 마스터 1.4641. **7월 관리회계 확정치를 확정실적 탭에 입력하면 분기 AM=1.3046+0.1595=1.4641, 분기 17.05·기 115.81로 완전 수렴**(구조상 정합, 검증 완료).
- 무회귀: 금일 0.4981·전일 잔고·총계좌 유지. 원장 원본 보존(주말 규칙은 합산 시 적용) → 저장 JSON 무영향.

## 추가 개선 (2026-08-06, 21차) — 주말 AM 이월을 '보고 윈도(금주+전주)'로 한정 → 분기·기까지 정답지 완전 일치

- **사용자 지시**: 차이가 나면 원인을 찾아 수정하라. 7월 관리회계 미확정이므로 그 전까지는 로데이터 합계값을 쓰는데, **7월 로데이터 합계값을 검증하라**.
- **Back 파일 7월 산식 확인**: 분기 = `7월(Q) + 8월(N)`, 7월 Q = **`SUM(AB:BF)` = 7월 일별 합**(daily 컬럼). 즉 정답지 7월도 '로데이터 일별 합'이다.
- **원인 규명(일자별 대조)**: 도구가 **7월 오래된 주말 6일(7/4·5·11·12·18·19)**에 AM 관리보수를 채워 정답지 대비 +0.0708 과다. Back은 주말 AM 상수를 **금주(8/1·2)+전주(7/25·26)만** 채우고 그 이전 주말은 0으로 둔다(daily 유지 범위 = WoW 주간보고의 금주·전주).
- **수정**: `dayFlowRaw` AM 주말 특례에 **보고 윈도 경계** 추가 — `REPORT_DAY` 기준 전주 토요일(=금주 토요일−7) 이전 주말은 상수 미이월(0). 금주·전주 주말만 관리보수 상수 이월.
- **정답 대조(8/5) — 5개 구간 전부 완전 일치**:
  - 금일 0.4981 · 주간 2.00165 · 월간 2.00165 · **분기 17.04965348837567** · **기 115.80838…** = 정답지와 소수 16자리까지 동일.
  - AM 분기 1.4641 · 기 7.4705(정답 정확), 전 포켓(BK·여신·예금·SW·IB·기타·IB FEE) 전 구간 일치.
- **적용환율 검증**: 도구 fxOf = Back 23열(일자별 F/X)과 15자리까지 동일(8/1~8/5=11.650361967883944, 7/29~31=11.624166880075327). 주차별 '전주까지 YTD 기누적환율' 정상.
- 무회귀: 금일 0.4981·총계좌 92,540 유지. (전일 잔고는 주말환율 주차통일 규칙 반영분.)
- 7월 관리회계 확정치 입력 시에도 정합(확정 blend가 raw 대체) — 현 시점 raw 합계 기준 완전 일치.

## 추가 개선 (2026-08-06, 22차) — 정합성 최종 확인 + 보고양식 표 열너비 정렬

- **5개 구간 정합성 최종 확인**: 금일 0.4981 · 주간 2.00165 · 월간 2.00165 · 분기 17.04965 · 기 115.80838 = 정답지와 완전 일치. 내부관계(주간=월간, 분기≥월간, 기≥분기, 금일≤주간)도 정상.
- **결과 페이지(보고양식) 표 열너비 정렬**: 순영업수익 현황·외형/KPI 표가 auto-layout이라 실적(57~74px)·달성률(81px) 열폭이 들쭉날쭉했던 것을 `table-layout:fixed`+`colgroup`으로 **5구간×(실적/달성률) 10열을 동일폭**으로 정렬(구분 13%·기목표 9%·구간열 각 7.8%). 신용대출·계좌수 표도 colgroup으로 고정.
- 계산 로직 무영향(표시 전용). 엑셀 2시트·정답 대조 재확인.

## 추가 개선 (2026-08-06, 23차) — 확정실적 12월까지 입력란 + 적용환율 기간 표기 + 반환 파일명 날짜

- **확정 실적 입력란 12월까지**: `renderClose`가 당월까지만 만들던 것을 **회계연도 12개월(1~12월) 전체**로 확장. 당월 이후(9~12월)는 빈 미마감 슬롯으로 미리 배치(마감 시 채워넣기). 계산은 보고월까지만 반영하므로 미래월 입력이 있어도 무영향.
- **적용환율 기간 표기**: 순영업수익 현황 우상단 적용환율에 **무슨 환율·어느 기간 기준인지** 병기 — 예: `적용환율 11.650362 IDR/KRW (기누적환율 · 주차고정 · 전주말 2026-07-31까지)`. 환율방식(주차고정/일자별/기준일)에 따라 문구 자동 변경.
- **반환 파일명 날짜**: HTML 산출물은 `인도네시아_일일보고_자동화_YYMMDD.html`(예: _260805) 형식으로 날짜를 붙여 전달.
- 계산 로직 무영향, 정답 대조·엑셀 재확인.

## 추가 개선 (2026-08-06, 24차) — 사용설명서(매뉴얼) 현행화

- `인도네시아_일일보고_사용설명서.docx`를 현재 버전에 맞춰 갱신(기존 서식·표 유지, 본문만 교정):
  - 인트로: 산출 숫자가 본사 집계 시트(정답지)와 일치하도록 맞춰져 있고, 미확정월은 원천 합계로 계산·확정 입력 시 수렴함을 명시. (2026-08-06 기준판)
  - 산출물/엑셀: 보고양식 엑셀이 **일일보고+세부 일일보고 2시트**임을 반영.
  - 요일별/주말: 주간=토~금(토요일 시작), 주말 BK·IB FEE·SW=0·**AM은 관리보수(AM Rev) 상수 이월**, 이자성 항목 월요일 3일치, **주말 환율=전주까지 기누적환율 주차고정** 설명 추가.
  - 확정 실적: 입력란이 **회계연도 1~12월 전체** 미리 배치(마감월별로 채워넣기)임을 명시.
  - 보고 양식: 순영업수익 현황 우상단 **적용환율에 기준(기누적·주차고정·전주말 날짜) 병기**, 표 실적/달성률 열 동일폭 정렬 안내.
- 문서 XSD 검증 통과(문단 329 유지).

## 추가 개선 (2026-08-06, 25차) — 기본 보고일 = 마지막 확정영업일의 다음 영업일 (불일치 사고 차단)

- **문제**: 사용자 화면에서 금일 0.46·주간 2.10이 나와 정답지(0.50/2.00)와 불일치. 원인 추적 결과 **기본 보고일이 8/6으로 잡히고, 8/6은 목요일이라 [오늘 쓸 숫자]가 자동으로 '주간 추정'으로 전환**되어 금일이 8/5 추정치(0.46)가 된 것. 실제 브라우저로 빌드별·설정별 전수 실행해 확인(금일 0.46·달성률 63.4%·BK 0.21/96.1%·BK 주간 0.76이 8/6 실행과 소수점까지 일치).
- **수정**: 기본 보고일을 **원천 파일명의 as-of 일자(DDMMYY)의 다음 영업일**로 산정. 예: `Daily_reportKISI_040826` → 8/4 확정 → 기본 보고일 **8/5**(금일=8/4 확정치). 로데이터에 들어있는 8/6·8/7 예상치는 기본값으로 잡히지 않음. 파일명에서 못 읽으면 기존 방식('오늘 이하 최신 자료일')으로 폴백. `srcAsOfDate`·`nextBizDay` 신설. SRCNAME은 로데이터(xlsx/xlsm)에만 설정되므로 JSON 파일명 오파싱 경로는 없음.
- **검증(실제 Chromium으로 배포 HTML 구동)**: 로데이터만 넣으면 기본 보고일이 **2026-08-05**로 자동 설정되고 **원천 모드**로 KISI 계 **0.50 / 2.00 / 2.00 / 17.05 / 115.81** = 정답지 일치. 설정 6조합(추정모드·주말채움·환율방식)에서도 8/5는 전부 2.00 유지.
- **검증 방식 시정**: 이전까지 Node harness에서 `buildReport()`를 직접 호출한 결과만으로 '일치'를 판단했는데, 화면에는 기본 보고일 자동선택·요일별 모드 전환이 더 얹힌다. 앞으로는 **실제 브라우저로 배포본 HTML을 구동한 산출물**로 대조한다.

## 추가 개선 (2026-08-06, 26차) — 보고일 규칙 확정: 전일까지 확정 · 금일 = 전일 확정치 (요일 자동 추정전환 제거)

- **사용자 확정 규칙**: "보고일자를 선택하면 그 날짜 기준으로 **전일까지가 확정**이고, **금일은 사실상 전일의 확정치를 그대로 사용**한다."
- **충돌 지점 제거**: `autoMode = (목·금) ? "추정" : "원천"` 때문에 목·금 보고일에서 [오늘 쓸 숫자]가 **자동으로 '주간 추정'으로 전환**되어 금일 값이 바뀌었다. 금일이 항상 전일 확정치라면 요일이 금일 값을 바꿔선 안 되므로 **자동은 항상 '원천'**으로 고정. 주간 추정 파일은 사용자가 [오늘 쓸 숫자]에서 **직접 고를 때만** 사용.
- **결과(실제 Chromium 구동 검증)** — 보고일만 바꾸면 규칙대로 따라감:
  - 보고일 **8/5** → 금일상한 8/4 · 금일 = 8/4 확정치 → KISI **0.50 / 2.00 / 2.00 / 17.05 / 115.81** = 정답지 일치
  - 보고일 **8/6**(목) → 금일상한 8/5 · 금일 = 8/5 확정치 → 0.46 / 2.43 (규칙대로), **원천 모드 유지**
- 이제 보고일 선택이 산출값을 온전히 결정하며, 요일에 따라 숫자가 조용히 바뀌는 경로가 없다.

## 정정 (2026-08-06, 27차) — '7월 관리회계 미확정 탓' 설명은 오류였음 + 7월 일별합산 검증

- **정정**: 20차 항목의 "분기·기 잔차는 7월 관리회계 미확정 raw 추정 탓(확정 입력 시 수렴)"은 **틀린 설명**이다.
  그 시점 잔차의 실제 원인은 **AM 주말 관리보수 이월 범위**였고(21차/9777d09에서 보고 윈도로 한정해 해소), 7월과 무관했다.
- **실제 운영 기준(사용자 확인)**: 월 관리회계 마감은 **다음달 2주차쯤** 완료된다. 그 전까지는 정답지도 도구도 **일별 데이터 합산값**을 쓴다. 7월은 현재 미마감이므로 확정치를 넣을 것이 없다.
- **검증(배포본 코드로 실측)**:
  - 확정실적(CLOSE) 등록월 = 2026-01~06 **뿐**, 2026-07 미등록 → 7월은 `blendEok`의 미마감월 경로로 **일별 합산** 계산.
  - 도구 7월(=분기 − 월간) vs Back 파일 `7월 실적(회계 전)` 열: BK 4.6647 · AM 1.3046 · IB FEE 0.0458 · 신용 4.0237 · 예금 0.4435 · SW 1.2621 · IB BOOK 3.2320 · 기타 0.0716 → **8개 포켓 전부 소수 4자리 일치**, 합계 **15.0480 = 15.0480**.
  - 최종 구간: 금일 0.4981 · 주간 2.0017 · 월간 2.0017 · 분기 17.0497 · 기 115.8084 = 정답지 일치.
- 7월 마감이 완료되면 확정 실적 탭에 넣는 순간 일별 합산을 대체한다(구조상 정합). 그 전까지는 입력 불필요.

## 긴급 수정 (2026-08-06, 28차) — 시간대(KST) 날짜 파싱 오류: 원천 일자가 하루씩 밀리던 문제

- **증상(사용자 보고)**: 보고일 8/5로 지정해도 금일 0.46 · 주간 2.10 · 분기 16.91 · 기 115.67 (정답 0.50/2.00/17.05/115.81). 원장 [불러온 데이터]의 원천 최신일자가 8/6으로 표시되고, 8/4 행에 8/5 값이 들어가 있었음.
- **근본 원인**: `dstr`/`iso`가 엑셀 날짜 셀(Date)을 UTC 성분으로 읽었는데, **SheetJS가 시간대·직렬값 변환 오차로 날짜를 자정 직전으로 생성**한다. KST(UTC+9)에서 2026-08-07 셀이 실제로는 `2026-08-06 23:59:08`(=`2026-08-06T14:59:08Z`)로 만들어져 **모든 원천 일자가 하루씩 앞당겨짐**. 개발 컨테이너가 UTC라 재현되지 않았고, 그래서 이전 검증이 전부 통과했던 것.
- **재현**: Playwright `timezoneId:'Asia/Seoul'`로 실행하니 사용자 화면과 **모든 숫자가 정확히 일치**(최신일자 8/6 · 금일 0.46 · 주간 2.10 · 분기 16.91 · 기 115.67 · BK주간 0.76 · AM주간 0.12 · 신용주간 0.64).
- **수정**: `dayLocal(d)` 신설 — 날짜를 **가장 가까운 자정으로 반올림**(12시 이후면 다음 날)한 뒤 로컬 성분으로 판독. `dstr`(원천 일자)과 `iso`(환율·휴일·추정 파일 일자) 모두 이 함수를 사용. 시간대와 무관하게 동일한 날짜가 나온다.
- **검증**: UTC · **Asia/Seoul** · America/New_York 세 시간대 전부 — 최신일자 2026-08-07, 기본 보고일 2026-08-05, 금일 0.50 · 주간 2.00 · 분기 17.05 · 기 115.81 **정답 일치**. KST에서 원장 BK 8/4=195.4 · 8/5=249.8 정상, 보고양식 **45칸(9행×5구간) 전수 일치**, 적용환율 11.650362 정상.
- 이 버그는 환율 원장·휴일·주간추정 파일의 일자 판독에도 동일하게 영향을 줬으므로 함께 해소됨.

## 추가 개선 (2026-08-06, 29차) — 보고일 지정을 무조건 존중(과거·미래 모두), 임의 변경 제거

- **요청**: 최신일자는 작업일 기준이 맞지만, **보고할 날짜는 사용자가 지정한 날짜가 그대로 결과물에 반영**되어야 한다. 과거일 수도, 미래일 수도 있다.
- **수정**: `recalc`에서 지정 보고일이 원장 시작일보다 이르면 **말없이 기본값으로 덮어쓰던 로직 제거**. 이제 지정한 날짜를 그대로 사용하고, 범위 밖일 때는 [적용 상태] 줄에 안내만 표시한다.
  - 원장 시작일 이전 → `원장 시작일(…) 이전 — 실적이 비어 있을 수 있음`
  - 원천 최신일자 이후 → `원천 최신일자(…) 이후 — 자료 없는 날은 직전 영업일 값으로 이월`
- **검증(KST, 실제 브라우저)** — 지정일이 모두 유지되고 구간이 일관되게 따라감:
  | 지정 보고일 | 유지 | 금일상한 | 금일 | 주간 | 분기 | 기 |
  |---|---|---|---|---|---|---|
  | 2026-01-05(과거) | ✔ | 01-04 | 2.20 | 2.20 | 4.20 | 4.20 |
  | 2026-07-31 | ✔ | 07-30 | 0.65 | 3.49 | 15.09 | 113.85 |
  | 2026-08-05 | ✔ | 08-04 | 0.50 | 2.00 | 17.05 | 115.81 (정답 일치) |
  | 2026-08-07 | ✔ | 08-06 | 0.43 | 2.84 | 17.88 | 116.64 |
  | 2026-08-10(자료 밖) | ✔ | 08-09 | 0.84 | 0.84 | 18.66 | 117.42 + 안내 |
- 기본 보고일(마지막 확정영업일 다음 영업일)은 **비어 있을 때만** 채우므로 사용자의 지정을 덮지 않는다.

## 추가 개선 (2026-08-06, 30차) — '환율 차이' 점검 문구 정정 (오해 소지 제거)

- **문의**: 점검 결과에 "원천 환율 11.583026, 적용환율 11.650361967883944 … 어느 쪽을 쓸지 확정하세요"가 떠서 무슨 뜻인지 모르겠다. 환율 파일 미업로드 때문인가?
- **사실 관계**: 환율 파일 업로드와 무관하다. 도구에는 **2,473일치 환율 원장이 내장**되어 있어 기누적 평균환율을 스스로 계산한다. 두 값은 성격이 다른 값이다.
  - `원천 환율(fx_src) 11.583026` = 현지법인이 원천 엑셀에 자체 기입한 환율
  - `적용환율 11.650361967883944` = 회계연도 **기누적 평균환율**(주차 고정) — **Back 집계 파일 23행과 소수 15자리까지 동일**(검증 완료). 보고·집계 기준은 이쪽이 맞다.
- **수정**: 기존 문구가 "어느 쪽을 쓸지 확정하세요"라 사용자가 판단해야 하는 오류처럼 보였다. 다음과 같이 정정.
  - 차이 0.5% 미만 → `환율 일치`(pass)
  - 차이 3% 미만 → **`환율 기준 안내 (정상)`(pass)** — 보고는 기누적 평균환율을 적용하며 집계 시트 기준과 같고, 원천 값과 다른 것이 정상이며 **조치할 것이 없음**을 명시
  - 차이 3% 이상 → `환율 차이 큼`(warn) — 환율 원장(고시 누락·단위 오입력)과 회계연도 시작일 확인 안내
- 검증(KST): 환율 항목이 초록(정상)으로 표시되고, 보고값 0.50 / 2.00 / 2.00 / 17.05 / 115.81 불변.

## 버그 수정 (2026-08-06, 31차) — 보고 양식 PNG 저장 실패 (colgroup 회귀)

- **증상**: [보고 양식] 탭의 `그림(PNG)으로 저장`이 동작하지 않음(다운로드도 안내도 없음).
- **원인 1 (회귀)**: PNG는 `foreignObject` 안의 내용을 **XML로 파싱**해 만드는데, 22차에서 열너비 정렬용으로 넣은 **`<col>` 태그가 HTML void 요소**라 `outerHTML`이 자기닫음(`/>`) 없이 출력되어 XML 파싱이 깨졌다(`Opening and ending tag mismatch: col line ... and colgroup`). 즉 **표 열너비 정렬 작업이 만든 회귀**.
- **원인 2**: 크기를 `offsetWidth/offsetHeight`로 재서, 탭이 숨겨진 상태 등에서 0×0이 될 수 있었다.
- **수정**:
  - 복제 노드를 **`XMLSerializer`로 직렬화**해 void 요소가 XHTML 자기닫음 형태가 되도록 함(폴백: `outerHTML`).
  - CSS를 **CDATA로 감싸** `>`·`&` 등이 XML을 깨뜨리지 않게 함.
  - 크기를 `getBoundingClientRect` 기준으로 재고 `scrollWidth/Height`·기본값(920)으로 폴백.
- **검증(KST, 실제 브라우저 다운로드)**: 배율 **1배 920×912(151KB) · 2배 1840×1824(375KB) · 3배 2760×2736(634KB)** 모두 정상 생성. 생성된 PNG를 열어 표·서식·색상·값(0.50 / 2.00 / 2.00 / 17.05 / 115.81)까지 정상 렌더링 확인.

## 개선 (2026-08-06, 32차) — 주간 목표 점검을 보고 기준(토~금)으로 통일 + 용어 명확화

- **지적**: ① '금주 진행분'이 8/3~8/5로 나오는데 보고상 주간(토~금)이면 **8/1~8/5**여야 한다. ② '완료 주차 대비'라는 용어가 모호하다. ③ '직전 완료주차'도 7/31이 마지막인데 집계 기준이 달라 보인다.
- **원인**: `weeklyCheck`가 보고와 달리 **`mondayOf` 기준(월~일)**으로 주를 잡고 있었다. 8/5(수)면 금주=8/3~8/5, 직전 완료주차=7/27~8/2로 나와 보고의 토~금과 어긋났다.
- **수정**:
  1. **주간 정의를 보고와 동일한 토~금으로 통일**. 이번 주 토요일 = `addDays(asOf, -((dow(asOf)+1)%7))`, 직전 완료주차 = 그 전 주 토요일~금요일(보고일이 금요일이면 그 주가 완료).
  2. **용어 변경** — `완료 주차 대비` → **`① 지난주 마감 실적 vs 주간 목표 · (기간) (토~금)`**, `금주 진행분` → **`② 이번주 진행 실적 vs 경과 환산 목표 · (기간) (토~금 중 N일 경과)`**. 상단 설명도 두 표의 의미를 명시.
  3. **보고서와 수치 일치**: 금주 진행분에 보고서와 같은 **'당일 = 전일 확정치 이월'** 규칙을 적용(기존에는 미적용이라 BK 0.55 vs 보고서 0.50으로 어긋났음).
  4. 보고일이 **금요일**이면 이번주가 그날 마감되어 ①과 같은 주가 되므로, 중복 표 대신 `이번주는 오늘 마감되었습니다` 안내로 대체.
- **검증(KST, 실제 브라우저)**:
  - 8/5(수) → ① 2026-07-25~07-31, ② 2026-08-01~08-05(5일 경과) ✔
  - 8/3(월) → ① 07-25~07-31, ② 08-01~08-03(3일) · 8/8(토) → ① 08-01~08-07, ② 08-08(1일)
  - 8/7(금) → ① 08-01~08-07, ② 자리에 '오늘 마감' 안내
  - 수치 일치: 주간점검 이번주 BK 0.50 · AM 0.16 · SW 0.22 · IB Book 0.39 · 기타 0.06 = 보고서 주간과 동일(이자 0.66 = 신용 0.62 + 예금 0.05).
- 보고서 값(0.4981/2.0017/2.0017/17.0497/115.8084) 무회귀 확인.
